# Test File: 03_Claims_Menu_test.spec.ts

Module: Claim Menu on Dashboard search results
Location: tests/Claims/03_Claims_Menu_test.spec.ts
Test Data: testData/ClaimsMenuTestData.json
Fixture: loginAsAdmin from tests/myTestData.ts
Navigation Helpers:
1. navigateToClaimsDashboard from tests/framework/navigation.helper.ts
2. navigateToClaimsArchiveDashboard from tests/framework/navigation.helper.ts

## Test Cases Implemented

| # | Test Name | Type |
|---|-----------|------|
| 1 | Claim dashboard controls, search fields, and actions are visible and available | Visibility |
| 2 | Apply Filter by claim id returns a successful search result and claim row exists in DB | Functional + DB assertion |
| 3 | Worked action can be executed from row menu without UI runtime errors | Functional |
| 4 | Timely Filing report validates field visibility and DB-backed values | End-to-end + DB assertion |
| 5 | Timely Filing report validates additional optional DB-to-UI fields when present | End-to-end + conditional DB assertion |
| 6 | Empty filter values still keep search flow stable when Apply Filter is clicked | Functional + Edge case |
| 7 | Invalid claim id returns no results in UI and null from DB utility | Edge case |
| 8 | Whitespace claim id does not crash the page and keeps Apply Filter available | Edge case |
| 9 | Claims Archive dashboard navigation is available from Claims module | Navigation |

## DB Query Coverage

Primary query (implemented in database utility):

select inputfilename, inputseqnumber, providerid, statusflag, batchstatus, payerid, processorid,
       patientname, dateofservice, patientaccountnumber, modeinfo, hintimestamp, insuredname,
       transmitfilename, totalcharges, inputformat, outputformat, inputclaim, rejecterrors,
       warningerrors, processcode, patientbirthdate, patientgender, patientrelationship,
       insuranceplan, reportid, reportfilename, billed, statementdate, routingmethod,
       payerzipcode, sop, claimid, batchnumber, formattype, outputbatchnumber,
       carrierprocessdate, carrierprocesstime, claimstatus, billingnpi, billingtaxid,
       renderingnpi, providerlastname, providerfirstname, providermi, attachments,
       billingproviderlastname, billingproviderfirstname, billingprovidermi, neicid,
       filename277ca, filename277u, xmlfilename, insuredid, patientlastname,
       patientfirstname, patientmi, ediid2, ediid3, payerresp1, payerresp2, payerresp3,
       paymentfilename, csvfilename, claimstatusfilename, mappedoutput,
       interchangesenderid, applicationsendercode, numberofattachments
from sc_app_archive.claims
where claimid = 'G2349622081814030403'

Implemented DB helper:
1. fetchClaimArchiveMenuRowByClaimId(claimId)

## Reusable Helpers in Spec

1. openClaimDashboard(page)
2. applyFilterAndWait(page)
3. getEnabledTextbox(page, name)
4. trySetFilterValue(page, name, value)
5. clearClaimFilters(page)
6. searchByClaimId(page, claimId)
7. openRowActionMenu(page)
8. openTimelyFilingReportForRow(page)
9. markRowAsWorked(page)
10. assertTimelyFilingLabelsVisible(page)
11. assertTimelyFilingValuesMatchDb(page)

## Notes

1. Login steps generated by recorder were refactored to use fixture-based authentication.
2. Hardcoded UI values were moved into testData/ClaimsMenuTestData.json and consumed by key.
3. The recorded Claim Menu flow is preserved in the spec as a reference comment and implemented through reusable functions.
4. afterEach validates that no browser runtime page errors were thrown.
