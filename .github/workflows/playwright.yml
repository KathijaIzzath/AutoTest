name: Playwright Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    # Runs daily at 19:30 UTC (after 7 PM UTC)
    - cron: '30 19 * * *'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      DAILY_REPORT_DIR: playwright-report/daily-summary

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Typecheck
        run: npm run typecheck

      - name: Run Playwright tests (scheduled daily full suite)
        if: github.event_name == 'schedule'
        run: npm run test:daily:full

      - name: Run Playwright tests (full suite)
        if: github.event_name != 'schedule'
        run: npm run test:full

      - name: Upload daily summary HTML
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: daily-test-summary
          path: playwright-report/daily-summary/
          if-no-files-found: ignore

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          if-no-files-found: ignore

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: test-results/
          if-no-files-found: ignore
